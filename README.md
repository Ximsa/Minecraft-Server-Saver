# Server Saver
Server-only mod which allows for configuration of the server tickrate when players are present or absent.
Defaults are 20 tps when players are present, 1 tps when idle.

Requires [TickrateChanger](https://github.com/Guichaguri/TickrateChanger)  mod 
